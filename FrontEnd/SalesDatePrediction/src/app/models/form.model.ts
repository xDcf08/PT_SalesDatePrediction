export type Employees = {
  empId: number;
  fullName: string;
}

export type Shippers = {
  shipperId: number;
  companyName: string;
}

export type Products = {
  productId: number;
  productName: string;
}